#include <stdio.h>

int main() {
   int a[50],b[50],c[100];
    
    
    
    int s1 = sizeof(arr1) / sizeof(arr1[0]);
    int s2 = sizeof(arr2) / sizeof(arr2[0]);
    int s3 = s1 + s2;
    
    
    int merged[size3];
    int i, j;

    
    for (i = 0; i < s1; i++) {
        c[i] = a[i];
    }

    
    for (j = 0; j < s2; j++) {
        c[s1 + j] = b[j];
    }

    
    printf("Merged Array: ");
    for (i = 0; i < s3; i++) {
        printf("%d ", c[i]);
    }
    printf("\n");

    return 0;
}
